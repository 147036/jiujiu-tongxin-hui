# 2026-08-28 工作日志

### H5 邀请函字体调整
- 用户反馈小字太小看不清
- 将所有小字体加大：副标题 12→14px、引言 11→13px、滚动提示 10→12px、核心路径说明 10→12px、介绍卡片标题 15→16px 正文 11→13px、邀请徽章 9→11px、正文 13→15px、引文 10→12px、表单标签 11→13px、输入框 13→15px、表单提示 10→12px、错误提示 10→12px、成功文字 12→14px、底部 10→12px
- 已重新部署到云端
## 追加
- 删除底部「查看报名汇总」链接，保护报名隐私

### 更新活动时间地点
- 用户要求添加活动时间地点信息
- 时间：2026年9月9日 13:00
- 地址：宝山区盘古路727号1号楼
- 在活动邀约区下方新增信息卡片，含时钟和定位图标
- 已重新部署

### 替换封面logo
- 用户要求将封面'善'字印章替换为'善和家族'logo
- Logo路径：C:/Users/30516/Desktop/图层 1.png
- 已复制到 assets/logo.png
- 更新CSS样式，使用<img>标签替代原来的文字印章
- 保留光环脉冲动画效果
- 已重新部署

### 添加背景音乐
- 用户要求添加轻音乐背景
- 网络下载遇到403错误，提供手动下载指引
- 推荐来源：爱给网、Pixabay Music、12134国风配乐
- 已更新HTML添加音乐播放器（右上角播放按钮）
- 音乐文件路径：assets/bg-music.mp3

### 替换背景音乐并实现自动播放
- 用户指定本地 mp3：C:/Users/30516/Downloads/P-mzpy04196-CD41D626.mp3（2.88MB），已覆盖 assets/bg-music.mp3
- 实现打开即自动播放：<audio> 加 autoplay/playsinline；JS 在 DOMContentLoaded 调 play()，微信环境监听 WeixinJSBridgeReady 触发，普通浏览器被拦截时退化为首次 touch/click 兜底；音量 30%
- 部署入口：复制 invitation-v8.html 为 index.html 后部署根目录
- 新链接：https://7c2e73dea0b54e9bbb367bd6576969a2.app.workbuddy.link（旧链接 2bf2f8... 内容已过时，建议停用）

### 开场引导层（解决微信自动播放失效）
- 微信系统级拦截 audio autoplay，纯 H5 无法保证打开即响；采用首屏「开启」引导层方案
- 新增 #introOverlay 国风扉页：logo 光环 + 品牌/标题 + 「开启」脉冲按钮 + 提示语
- enterInvitation()：play 音乐(setPlayingUI) + 扉页淡出缩放(.hide) + 首屏 fadeUp 入场(.reveal-in)
- 点击「开启」即合法用户手势，微信内 100% 可触发播放，从根本上绕过拦截
- 同一 sandbox 重新部署，链接不变

### 扉页与正文动效优化
- 扉页标题「九九立德 · 同心会」强制单行：font-size 32→25px、letter-spacing 8→4px、white-space:nowrap
- 扉页提示语「轻触开启 · 共赏雅乐」精简为「轻触开始」
- 正文三屏(.section-block)新增滚动入场动效：整体 fadeUp + 内部标题/卡片 stagger 依次淡入；卡片 hover 上浮
- 动效触发：IntersectionObserver(threshold .15) + scroll 兜底 checkReveal（进入视口 85% 内才显示），避免不显示且保留滚动动画
- 重新部署，链接不变

### 表单提交去向澄清 + 旧链接下线
- 用户确认表单数据是否同步到腾讯文档：查代码确认 H5 表单 action 为 FormSubmit 邮箱（https://formsubmit.co/3051685727@qq.com），数据只发到邮箱，不会进腾讯文档
- 腾讯文档表单（docs.qq.com/form/page/DZHBBYU5SYWhYc05r，file_id dpAaNRahXsNk）是当时单独给同事看的另一份表，与 H5 未打通
- 已下线旧链接 2bf2f80062224c91ab0a8732ebd59268.app.workbuddy.link（unpublish 成功，不再可访问）
- 现唯一有效链接：https://7c2e73dea0b54e9bbb367bd6576969a2.app.workbuddy.link

### 报名表单恢复为国风样式输入界面
- 用户强烈反馈不要嵌入表格，要求恢复好看的表单界面
- 已恢复：姓名/手机/组别下拉/备注 + 国风按钮，提交静默发后端API + 页面内显示"报名成功"
- 数据接收方式：提交到 /api/submit，存储在 data/entries.json
- **重要**：用户决定注册 Vercel 部署后端，不再使用 CloudStudio 嵌入方案

### 优化后端代码适配Vercel部署
- 创建 api/submit.js（POST 提交）和 api/entries.js（GET 查询）两个Serverless函数
- 优化 backend/server.js 支持本地开发和静态文件服务
- 添加 vercel.json 配置文件（支持Serverless路由）
- 创建 package.json 项目配置
- 创建 README.md 部署说明文档
- 数据文件路径改为 .data/ 目录（兼容Vercel临时文件系统）
- 已测试本地运行正常，API 响应正确
- 等待用户注册Vercel后上传部署

### 搭建后端收集系统（进行中）
- 用户要求搭建简单后端收集用户填写的信息
- 已创建 Node.js 后端服务（backend/server.js），提供 REST API：POST /api/submit、GET /api/entries
- 已创建管理后台页面（backend/admin.html），支持查看报名列表、导出CSV、清空数据
- 数据存储在 backend/data/entries.json 文件中
- 本地测试成功：提交和数据查询均正常
- **待解决**：CloudStudio 部署工具只支持静态网站，需要找到能运行 Node.js 的后端托管方案
- 备选方案：Vercel Serverless Functions、Railway、Render、腾讯云的 SCF 等
